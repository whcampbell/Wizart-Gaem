#pragma once
#include "../audio.h"

/**
 * INTENRAL USE ONLY
 * adds a sound to the list of playing sounds
 */
namespace hnd_sfx {

    /**
    * 
    */
    void addSound(Sound* sound, AudioContext ctx);
}